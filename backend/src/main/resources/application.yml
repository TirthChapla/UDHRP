spring:
  application:
    name: doctor-ai-backend
  
  # Database Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/doctor_ai_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Tirth2802
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false
  
  # File Upload Configuration
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  
  # Mail Configuration (Update with actual credentials)
  mail:
    host: smtp.gmail.com
    port: 587
    username: codingkamr14@gmail.com
    password: cqty fpka kjfi qglm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /api
  error:
    include-message: always
    include-binding-errors: always

# JWT Configuration
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000 # 24 hours in milliseconds

# Application Configuration
app:
  name: Doctor AI
  version: 1.0.0
  base-url: http://localhost:8080/api  # Change this to your production URL
  frontend-url: http://localhost:5173  # Change this to your frontend URL
  allowed-origins: http://localhost:5173,http://localhost:3000

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

# Logging Configuration
logging:
  level:
    com.doctorai: DEBUG
    org.springframework.web: INFO
    org.hibernate: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
